plugins {
    id "com.android.application"
}

android {
    namespace "projet.fst.ma.app"
    compileSdk 35

    defaultConfig {
        applicationId "projet.fst.ma.app"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation platform("org.jetbrains.kotlin:kotlin-bom:1.8.22")
    implementation "androidx.appcompat:appcompat:1.7.0"
}
